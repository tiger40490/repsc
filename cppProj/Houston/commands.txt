## This is a sequence of commands sent to the engine ##
use Engine3

# load a tiny tick file
nextTickFile inputTiny.csv

# print per-symbol statistics
stats

# use hard-coded numbers to verify the stats
verify

# load-test using a big tick file
# The async functionality is experimental
nextTickFileAsync input.csv

# print stats as the big tick file is loading, sometimes showing partial results
stats

# sleep that many microseconds
sleep 30

# Usually shows full results:
stats
