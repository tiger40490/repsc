todo: Engine2 to use 3D array

--command interface--
The engine will listen to commands on stdin.

One command will be "tickfie abc.txt" to support simple data loading, in incremental mode.

--load test--
a python script to send data at a certain rate on stdin

--Trade-offs--
