--todo: 
async command processing. While worker thread is processing ticks, a query thread can print stats

simulate heavy load without a huge file -- nextTickFile command to specify a sleep 

--numeric test--
See AbstractEngine::simpleTest() function

--command interface--
The engine will listen to commands on stdin. The attached command.txt lists the commands used in my test.

--Trade-offs--
See comments above Engine3 class.

--time spent-- 
3H 

--git repo--
I have no time to install and learn git at the moment.
